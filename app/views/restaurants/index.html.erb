<div class="container">
  <h1>Restaurants list: </h1>

  <ul class="restaurants-list">
      <% @restaurants.each do |restaurant|%>
    <li>
        <p><strong> <%= link_to restaurant.name, restaurant_path(restaurant) %></strong>
        <%# <%= link_to 'Edit', edit_restaurant_path(restaurant) %>
        <%# <%= link_to "Delete", restaurant_path(restaurant), method: :delete, data: {confirm: 'Are you sure you want to delete?'}  %>
        </p>
    </li>
      <% end  %>
  </ul>

  <button> <%= link_to 'Add a new restaurant', new_restaurant_path %></button>

</div>
